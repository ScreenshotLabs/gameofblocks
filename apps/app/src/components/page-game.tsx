"use client";

import type { PropsWithChildren } from "react";

export function PageGame({ children }: PropsWithChildren) {
  return (
    <div className="">
      <div className="">{children}</div>
    </div>
  );
}
